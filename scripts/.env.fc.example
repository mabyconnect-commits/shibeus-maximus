# Copy to scripts/.env.fc and fill in. scripts/.env.fc is gitignored.
# (Devnet values from setup are fine here; generate fresh ones for mainnet.)
VERCEL_TOKEN=
TREASURY_SECRET=
FC_MASTER_SEED=
FC_SESSION_SECRET=
# From a free Upstash Redis DB (console.upstash.com → your DB → REST API):
KV_REST_API_URL=
KV_REST_API_TOKEN=
# optional
SOLANA_RPC_DEVNET=https://api.devnet.solana.com
